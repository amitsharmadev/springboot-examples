server:
  port: 8080
  
 
spring:
  application:
    name: eureka-client
    
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8700/eureka
