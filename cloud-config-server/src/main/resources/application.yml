server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/amitsharmadev/spring-cloudconfig.git
          search-paths:
          - configuration/{application}/
          - configuration/
        

          

